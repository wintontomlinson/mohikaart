Fix the large white/blank gap appearing between the fixed navbar and the hero section on page load.

Requirements:
- The hero section must start immediately below the navbar with ZERO extra white space.
- Keep the navbar fixed/sticky at the top.
- Remove any unnecessary margin-top, padding-top, translate-y, spacer divs, or min-height issues causing the gap.
- Ensure the layout works perfectly on both mobile and desktop.

Implement this structure:

<header className="fixed top-0 left-0 right-0 z-50">
  ...
</header>

<main className="pt-[98px] md:pt-[106px]">
  <HeroSection />
</main>

Hero section requirements:
- Use:
  className="relative min-h-[calc(100vh-98px)] overflow-hidden"
- Prevent layout shift while images load.
- If using Next.js Image component:
  - use `priority`
  - use `fill`
  - use `sizes="100vw"`
  - use `object-cover`

Also inspect and remove:
- extra `mt-*`
- duplicate `pt-*`
- hidden spacer divs
- unnecessary flex gaps
- translate transforms
- empty wrappers creating height

Ensure:
- No CLS/layout jump on refresh
- Hero image renders immediately
- No blank area before hero content loads
- Smooth responsive behavior
- Clean production-ready structure

Do not redesign the page. Only fix the spacing/layout issue professionally.